[package]
name = "surreal_rocksdb"
publish = false
edition = "2021"
rust-version = "1.80.1"
license-file = "../../LICENSE"
resolver = "2"

[dependencies]
shared = { path = "../shared", features = ["rocksdb"] }

[lib]
crate-type = ["cdylib"]
